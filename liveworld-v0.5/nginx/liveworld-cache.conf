# Put this file at: /etc/nginx/conf.d/liveworld-cache.conf
# `conf.d/*.conf` is normally included inside Nginx's http {} context.
proxy_cache_path /var/cache/nginx/liveworld-celestrak
    levels=1:2
    keys_zone=liveworld_celestrak:20m
    max_size=250m
    inactive=8h
    use_temp_path=off;
